/*
1) Faça um programa que leia um nome e imprima as 4 primeiras letras
do nome.
*/
#include <stdio.h>
#include <string.h>
int main()
{
    int tamanho;
    char nome[100];
    printf("|Digite seu nome: ");
    fgets(nome,sizeof(nome),stdin);
    
    printf("Olá: %.4s", nome);
    
    return 0;
}
